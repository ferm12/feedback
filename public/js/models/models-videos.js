App.Models.Video = Backbone.Model.extend({
    defaults: {

    }
	// validate: function (attrs) {
	// 	if ( !attrs.first_name || !attrs.last_name ){
	// 		return 'A first and last name are required';
	// 	}
	// 	if (!attrs.email_address){
	// 		return 'Please enter a valid email address.';
	// 	}
	// },

	// initialize: function () {
        // console.log(this, 'this: comes from App.Model.Video ');
        // console.log(this.collection, 'this.collection: comes from App.Model.Comtact');
	// }
});

